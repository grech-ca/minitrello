import styled from '@emotion/styled';

export const StyledBoard = styled.div({
  padding: 10,
  display: 'flex',
  alignItems: 'flex-start',
  gap: 8,
  height: '100%',
  overflowY: 'hidden',
  overflowX: 'auto',
});
